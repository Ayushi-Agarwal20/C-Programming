#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    int length,inches;
    scanf("%d", &length);
    inches=length*12;
    printf("%d", inches);
    return 0;
}
